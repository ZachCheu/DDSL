import java.util.*;
import java.io.*;

public class RemoveOvr {
	public static void main(String[] args) throws FileNotFoundException {
		Scanner links = new Scanner(new File("links.txt"));
		PrintStream rLinks = new PrintStream(new File("newlinks.txt"));
		
		while (links.hasNext()) {
			String url = links.next();
			if (!url.contains("ovr"))
				rLinks.print(url + "\n");
		}
	}
}
